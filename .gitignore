# Build output (generated by `pnpm build`)
dist/
node_modules/
npm-debug.log*
.DS_Store
.vscode/
.remember/
# Secrets and local-only context — never committed or published
.dev.vars
.env
CLAUDE.md
*.tgz
# Planning docs (local only), matching the sibling libs
docs/
