{
  "name": "@dszp/autoelevate-lib",
  "version": "0.1.0",
  "description": "Portable, Node-free client for the AutoElevate Partner API (beta): read-only typed client, Bearer and HMAC signing, and a per-company active-agent count helper for billing reconciliation. Runs unchanged in a Cloudflare Worker, Node, or the browser.",
  "type": "module",
  "license": "MIT",
  "author": {
    "name": "David Szpunar",
    "url": "https://david.szpunar.com"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dszp/autoelevate-lib.git"
  },
  "bugs": {
    "url": "https://github.com/dszp/autoelevate-lib/issues"
  },
  "homepage": "https://github.com/dszp/autoelevate-lib#readme",
  "//publishConfig": "No `provenance: true` here on purpose: it would force provenance on EVERY publish, including the manual first one that a brand-new package requires (npm can only configure trusted publishing on a package that already exists). Provenance comes from the workflow's explicit `npm publish --provenance` in CI.",
  "publishConfig": {
    "access": "public"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "autoelevate",
    "cyberfox",
    "privilege-management",
    "msp",
    "integration",
    "cloudflare-workers"
  ],
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "//require": "ESM-only package. This `require` condition lets Node >=22.12 load it via require(esm); older Node still gets an accurate ERR_REQUIRE_ESM here instead of the confusing ERR_PACKAGE_PATH_NOT_EXPORTED the bare export map produced.",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    },
    "./package.json": "./package.json"
  },
  "types": "./dist/index.d.ts",
  "module": "./dist/index.js",
  "main": "./dist/index.js",
  "sideEffects": false,
  "//files": "The maps and `src` ship together on purpose: tsc writes a sourceMappingURL pointer into every dist file and the .d.ts.map references ../src/*.ts, so a Worker stack trace names a real .ts line and Go-to-Definition opens actual source. Tests and the mock-fetch harness are excluded by negation so they stay beside the code they test without shipping.",
  "files": [
    "dist/**/*.js",
    "dist/**/*.d.ts",
    "dist/**/*.map",
    "src",
    "!src/**/*.test.ts",
    "!src/testkit.ts",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch",
    "typecheck": "tsc -p tsconfig.test.json --noEmit",
    "//verify": "Build, then actually IMPORT the built entry point with Node. `pnpm build` and `vitest run` both pass on a dist that cannot be loaded (tsc emits relative specifiers verbatim; vitest resolves extensionless ones). This checks the artefact a consumer actually gets.",
    "verify": "pnpm build && node --input-type=module -e \"import('./dist/index.js').then(m => { if (!Object.keys(m).length) { throw new Error('dist/index.js exported nothing'); } console.log('verify: dist imports cleanly,', Object.keys(m).length, 'exports'); })\"",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3"
  },
  "packageManager": "pnpm@11.11.0"
}
